#ifndef EULERCACHES_H
#define EULERCACHES_H

#define NPRIME_MAX 19999999

int collatzCache[1000001];      /* Caching collats results as duplicates
                                   appear */

char primeCache[20000000];      /* Caching primes as they are calculated
                                   through out the problems. */

#endif /* EULERCACHES_H */
